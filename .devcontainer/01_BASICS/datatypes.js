"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    + 3) // code readability should be high this should not be used although output will be there but good practice in coding

console.log("ayush")

let name = "ayush"
let age = 30
let isLoggedIn = false
let state;

// number => 2 to the power 53 
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol(unique) used in react framework
// object

console.log(typeof undefined); // undefined 
console.log(typeof null); // object interview question